name: Build Project

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-24.04, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: 'recursive'
          fetch-depth: 0

      - name: Install build tools
        if: matrix.os == 'ubuntu-24.04'
        run: sudo apt-get update && sudo apt-get install -y cmake libsdl2-dev libcurl4-openssl-dev autoconf automake libtool autoconf-archive libltdl-dev

      - name: Install build tools (macOS)
        if: matrix.os == 'macos-latest'
        run: brew install cmake sdl2 curl autoconf automake libtool

      - name: Build project
        run: make simulator

      - name: Package build artifacts
        run: |
          mkdir -p dist
          if [ "${{ matrix.os }}" = "macos-latest" ]; then
            zip -r dist/lvgljs-macos.zip build/lvgl
          elif [ "${{ matrix.os }}" = "ubuntu-24.04" ]; then
            zip -r dist/lvgljs-linux.zip build/lvgl
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: lvgljs-${{ matrix.os }}
          path: dist/*.zip

  release:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')

    steps:
      - name: Get version from tag
        id: get_version
        run: echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          name: Release ${{ steps.get_version.outputs.version }}
          draft: false
          prerelease: false
          files: |
            artifacts/lvgljs-macos/lvgljs-macos.zip
            artifacts/lvgljs-ubuntu-24.04/lvgljs-linux.zip